/**
 * Closure Section Data — "Der letzte Schritt"
 * 
 * German copy for the emotional permission section.
 * Calm, confident invitation to book — no pressure.
 */

export interface ClosureData {
  headline: string;
  subtext: string;
  ctaText: string;
  secondaryText: string;
}

export const closureData: ClosureData = {
  headline: 'Bereit für deinen Termin?',
  subtext: 'Ich freue mich auf dich.',
  ctaText: 'Termin buchen',
  secondaryText: 'Noch Fragen? Schreib mir.',
};

export default closureData;
