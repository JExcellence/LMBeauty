# Empty NEXT_PUBLIC_API_URL makes API calls relative to frontend domain
# This uses Next.js API routes at /api/* which provide fallback data when backend is unavailable
NEXT_PUBLIC_API_URL=
# Backend URL for server-side API routes to proxy to (when backend is deployed)
BACKEND_URL=https://api.lmbeauty.de
NEXT_PUBLIC_BACKEND_URL=https://api.lmbeauty.de
